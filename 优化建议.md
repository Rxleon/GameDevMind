# GameDevMind 仓库优化建议

> **最后更新**: 2026-06-20
> **说明**: 本文仅保留**尚未完成**的优化项；已完成内容见下方归档。

---

## ✅ 已完成（归档）

以下建议已落地，不再展开：

| 类别 | 内容 |
|------|------|
| 导航与索引 | 自动生成 `INDEX.md`；`nav/index.html` 交互式导航 + 全景图；三条阅读路径（`mds/topics/推荐阅读路径.md`）及路径 A/B/C 一键筛选 |
| README | 阅读说明、三条推荐路径、配套 code/cases/ai-cases 入口、交互式导航链接 |
| 配套资源 | 10 个代码示例、8 个实战案例、5 个 AI 对话；关键文档「延伸实践」块 |
| AI Coding | 全库技术文档已补 AI Coding 指南（交互/方法/应用/提示词范例） |
| 工具与协作 | Windows / 跨平台脚本；`tools/README.md`；Issue / PR 模板；贡献者展示 |
| CI | 链接检查、格式检查、索引自动生成 |
| 链接维护 | Unity Open Projects 失效链接已替换为 GitHub 仓库 |

---

## 🎯 待优化项

### 1. 文档格式规范统一化

**优先级：高**

**问题**：`CONTRIBUTING.md` 要求纯 Markdown（不使用 HTML 标签），但大量文档仍使用 `<h2 align="center">` 等 HTML 样式，规范与实际不一致。

**建议**（二选一）：

- **方案 A（推荐）**：更新 `CONTRIBUTING.md`，明确允许 HTML 标签用于标题居中、表格样式等，并同步 `format-check` workflow
- **方案 B**：批量将文档改为纯 Markdown（工作量大）

**关联**：`template/z.模板.md` 仍含旧格式与 `?raw=true`，需与最终规范对齐后完善模板。

---

### 2. 导航与索引（剩余）

#### 2.1 README 补充链接

**优先级：中**

README 已有阅读说明与三条路径，仍可补充：

- 指向 `INDEX.md` 的链接
- 指向 `HISTORY.md` 的更新日志链接
- （可选）FAQ 章节

#### 2.2 搜索体验

**优先级：低**

- 在 README 或 `阅读说明.md` 补充 GitHub 代码搜索技巧
- （可选）在 `INDEX.md` 增加关键词索引
- （长期）评估 MkDocs / VitePress / Docusaurus 静态站点，替代或增强 GitHub Pages

---

### 3. 工具脚本（剩余）

**优先级：低**

- 统一工具配置（`tools/config.json` 或 `config.yaml`）
- （可选）更多 PowerShell 脚本、统一 CLI 入口、脚本单元测试

---

### 4. 内容质量（剩余）

#### 4.1 扩展 CI / 本地检查

**优先级：高**

已有链接与基础格式检查，可继续扩展：

- 必填字段检查（关键词、标签等）
- 图片存在性检查（引用路径 vs 实际文件）
- 内容完整性检查（「是什么？在哪用？」等结构）
- 提供本地可运行的检查脚本，方便贡献者提交前自检

#### 4.2 配套资源持续扩充

**优先级：低**

已有 10 + 8 + 5 配套资源，可按图谱覆盖度继续补：

- 更多文档与 code / cases / ai-cases 的映射
- 技术类文档中的内联代码片段与反例

#### 4.3 内容审核

**优先级：低**

- 内容审核清单
- 文档质量评分标准
- 定期审查与更新机制

---

### 5. 文档结构

#### 5.1 命名规范文档

**优先级：中**

当前以中文 + 数字编号为主，可单独写一页命名规范，避免新文档风格漂移。

#### 5.2 元数据与版本（可选）

**优先级：低**

- YAML Front Matter（title / category / tags / last_updated）
- 文档内版本号与变更记录

---

### 6. 图片资源

**优先级：中～低**

- PNG 压缩 / WebP、图片优化脚本
- 引用 vs 实际文件检测、未引用图片清理
- （可选）CDN / 懒加载

---

### 7. 多语言（可选）

**优先级：低**

- 与 `GameDevMind-EN` 保持同步
- README 语言切换已存在，可加强双向链接与更新节奏说明

---

## 📋 实施优先级

### 🔴 高优先级

1. **明确文档格式规范**：统一 CONTRIBUTING、模板、format-check
2. **扩展文档检查**：图片存在性、必填字段、本地检查脚本

### 🟡 中优先级

1. **README 补充**：INDEX.md、HISTORY.md 链接
2. **图片优化**：压缩与缺失检测脚本
3. **命名规范文档**

### 🟢 低优先级

1. GitHub 搜索说明 / 关键词索引
2. 静态站点生成器评估
3. 工具统一配置、元数据、CDN、FAQ 等

---

## 📝 备注

基础设施与主路径建设已基本完成，当前重点为**规范统一**、**检查自动化**和**体验细节**（索引链接、图片、搜索）。
